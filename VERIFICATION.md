# Luma Editor 1.0.0 验证记录

验证日期：2026-09-13。环境：macOS 26.6.2，Apple arm64；Windows 11 x64 构建通过 GitHub Actions `windows-latest` 执行。

## 已执行并通过

| 验证层次 | 结果 | 覆盖范围 |
| --- | --- | --- |
| TypeScript 检查与正式构建 | 通过 | 编辑器源码、依赖和本地 Worker 打包 |
| 格式化与存储检查 | 42 项通过 | 22 个内置语言模式、错误语法、换行符、缩进、外部命令超时/输出限制、并发、源文件保护、文件冲突、UTF-8/BOM、路径授权、草稿持久化 |
| 真实 Electron 界面流程 | 13 项通过 | 编辑/实时预览、格式化、撤销重做、保存、目录刷新、主题、快速打开、错误保护、崩溃恢复、取消/丢弃关闭、900×650 布局、正常退出 |
| 最终应用包中的格式化 | 12 项通过 | JavaScript、TypeScript、JSON、Java、PHP、XML、TOML、SQL、C、C++、C#、Swift |
| 原生 macOS 保存流程 | 通过 | 最终 `.app` 新建文档、输入中文及 Markdown 任务列表、系统保存窗口选择目录、写入磁盘、自动识别 Markdown、预览复核 |
| Windows 11 打包配置 | 已配置 | `npm run package:win` 生成 x64 NSIS 安装器和便携版；GitHub Actions 在 Windows runner 执行 npm test 与打包 |

真实界面流程测试记录的页面错误和控制台错误均为 0。最终应用包的格式化检查也没有页面错误。

界面自动化中的文件选择、保存及确认对话框以返回路径和选项的替身驱动；文档编辑和命令通过真实界面完成。随后另外使用最终应用及真实系统保存窗口验证了一次完整保存，并读取磁盘确认内容一致。

## 修复后复核的问题

- 应用重启时，无修改的标签重新读取磁盘内容，未保存草稿保留恢复。
- 最多 50 个标签页，避免超出会话保存限制。
- 已授权文件的父目录被删除后，其他草稿仍可保存；实际文件读取仍执行路径检查。
- 保存文件后，已展开文件夹的子项保持可见。
- 退出时如果取消后续文档的确认，不会提前丢弃前面文档的修改。
- Markdown 任务列表在安全预览中正确显示选中状态。

## 验证边界

没有验证所有语言、所有语法、所有第三方格式化工具或长时间大文件负载。Python、Go、Rust、Shell 在本机缺少相应工具，未执行这几种语言的本机格式化。应用未进行 Apple 开发者公证、跨机器分发测试或 Intel Mac 测试。

最终 `app.asar` SHA-256：

```text
c550ee229535ba4a939c2e924d3dc37467ad791b69cfd93a0b59e29ffa893544
```
